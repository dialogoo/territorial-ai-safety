# mdBook
/book/

# generated outputs from the standalone pipeline
/outputs/

# raw data: large and/or licence-bound — keep local, or version with DVC
/data/raw/

# python
__pycache__/
*.pyc
.venv/
.env
.ruff_cache/

# editor
.history/

# os
.DS_Store
desktop.ini

# NOTE: do NOT ignore /src/generated/ — it is committed so the book builds
# without Python, and so code/text drift shows up as a diff.
